import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['i.ibb.co', 'i.postimg.cc', 'ashutoshsundresh.com', 'raw.githubusercontent.com', 'www.freeiconspng.com', 'img.freepik.com'],
  }
};

export default nextConfig;
